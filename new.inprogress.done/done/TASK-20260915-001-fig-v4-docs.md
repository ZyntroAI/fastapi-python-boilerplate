---
id: TASK-20260915-001
title: Convert FIG_V4 pseudocode into docs/fig with real JSON config
status: done
priority: normal
created: 2026-09-15
updated: 2026-09-15
owner: fig-ai-agent
repo: ZyntroAI/fastapi-python-boilerplate
issue:
prs: []
blocked_by:
tokens: 0
---

# TASK-20260915-001 — Convert FIG_V4 pseudocode into docs/fig with real JSON config

## Goal

The FIG v4.1 Organization Edition specification currently exists only as
jsx-style pseudocode (`FIG.ORG = { ... }`) inside `.fix/FIG_V4/`. After this
ships, the spec is a set of machine-readable JSON config files under
`docs/fig/` that parse, validate, and can actually be loaded by tooling.

## Scope

- `docs/fig/` — README, ARCHITECTURE, governance, masterfiles, security,
  github-rules, metadata.
- `docs/fig/config/` — 8 JSON files transcribed from the source document,
  including a JSON Schema for audit events and a worked example.
- `docs/fig/validate_config.py` — re-runnable validator (34 assertions).

## Out of scope

- **Deleting `.fix/FIG_V4/`.** The original stays as the source of truth until
  someone confirms FIG is a code framework and not a specification. Removing it
  now would destroy the only evidence of provenance.
- **Writing `FIG.jsx`.** No such file exists in any repo, and the source does
  not say whether one should. See `docs/fig/metadata.md`.
- The `เอกสารเพิ่มเติม` file in the same folder — it covers book-to-skill,
  Skills-vs-MCP, and invoice audit, and is not about FIG.

## Steps

- [x] Read `.fix/FIG_V4/ZyntroAI_Organization/SKILL.md` as the source of truth
- [x] Transcribe every value into JSON config, inventing nothing
- [x] Write the prose docs around those config files
- [x] Write `validate_config.py` covering parse, value fidelity, and schema
- [x] Run the validator in place (34/34 passing)
- [x] Record open questions in `metadata.md` rather than silently deciding them

## Acceptance criteria

- [x] `python docs/fig/validate_config.py` exits 0 with all checks passing.
- [x] Every config value traces to a line in the source document.
- [x] No jsx pseudocode remains in any config file.
- [x] Unverified claims are labelled as unverified in `metadata.md`.
- [x] Existing tests still pass (markdown + JSON only; no code paths touched).

## Dependencies / blockers

None.
