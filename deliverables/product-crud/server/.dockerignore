node_modules
dist
*.tsbuildinfo

# Secrets and local overrides — never bake these into an image.
.env
.env.*
!.env.example

# Tests and tooling configs are not needed inside the image.
src/**/*.test.ts
vitest.config.ts
tsconfig.seed.json

# Build context noise.
.git
.gitignore
Dockerfile
.dockerignore
*.log
