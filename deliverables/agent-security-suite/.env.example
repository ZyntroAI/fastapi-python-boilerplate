# Agent Security Suite — copy into this directory as .env

# Where the audit log database is written (set by tests to a temp path).
AUDIT_DB_PATH=./audit.db

# RunPod API key used by runpod_client.py
RUNPOD_API_KEY=

# Incoming-webhook URL for security alerts
SLACK_WEBHOOK_URL=
