apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-core.fullname" . }}
  labels:
    {{- include "agent-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-core.selectorLabels" . | nindent 4 }}
