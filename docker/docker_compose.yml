version: "3.9"
services:
  web:
    build: .
    container_name: myapp_web
    ports:
      - "8080:8080"
    environment:
      - ENV=production
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 512M
