It sounds like you want the Node package equivalent of the CRAN/R idea—so you’re asking about an **`npm` package** (not `package.npm` as a package name).

Which one do you mean?

1) The name of a specific package on npm (tell me the exact name), or  
2) A package that provides similar functionality to **`sharp`** (stability selection / consensus clustering), or  
3) Just “how to install a package from npm” (e.g., using `npm install <name>`)?

If you mean (2), tell me whether you want stability selection (feature selection reliability) or consensus clustering (cluster robustness), and what data type you have (regression/classification/clustering, plus Python vs Node).
