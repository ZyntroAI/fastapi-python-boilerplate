## Version

v1.2.0

## What's included

Minor release cut from `main` at `3bf46e4`. Since `v1.1.0` (`d1ee77d`, 2026-09-09) — 9 commits.

**Added**
- **PR #212** — `docs/MCP-Guide-Complete.md` (MCP-DOC-2026-0912): Thai-language MCP troubleshooting & setup reference covering Google Cloud ADC, missing runtimes (Node.js / Dart / Go) and PATH, and third-party API keys. Plus `scripts/check-mcp-environment.sh` — automated checker with `--gcp` / `--runtimes` / `--keys` and a CI-suitable exit code that never prints secret values.
- **PR #206** — `fastapi-obsidian-backend` connected to a live Obsidian vault via the Local REST API: `app/obsidian/client.py` + `app/routers/obsidian.py`. Writes require JWT **and** `OBSIDIAN_ALLOW_WRITE=1`; vault-relative paths only; payloads sanitized through the CWE-1321 bridge. 33 tests.

**Fixed**
- **PR #214** — `check_key()` reported the length of the variable *name* (a constant 4) instead of the value.
- **PR #216** — `--help` leaked source (`sed -n '2,22p'` ran past the comment header); two stale paths in the MCP guide corrected.

**Dependencies**
- `7daddda` — bump the production-deps group with 3 updates.

**Docs**
- **PR #213 / #215 / #217** — CHANGELOG entries for the above, per repo convention.

## Changelog

Full section: [`CHANGELOG.md` → `[2026-09-12]`](CHANGELOG.md). Also spans `[2026-09-11]` (Obsidian bridge, OnSpaceAI platform integration, roadmap).

## Migration / breaking changes

None. No migration required.

## Verification

- [ ] CI green on `main` — see note below
- [x] Tests pass — `scripts/test_mcp_checker.sh` 17/17
- [ ] Migration applied (if any) — N/A

**CI note:** workflows on `main` currently fail at *Set up job* for two pre-existing reasons unrelated to this release — five workflow files are syntactically invalid, and all action refs must be pinned to full commit SHAs. The fix is prepared as a patch (`ci_sha_pin_workflow_fix`) but cannot be pushed: the GitHub App lacks the `workflows` permission. See P-001/P-003/P-009 in `PROBLEMS.md`.
