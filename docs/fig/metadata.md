# Metadata — ชุดที่มาและข้อที่ยังไม่ยืนยัน

## ที่มา

| ไฟล์ต้นฉบับ | ตำแหน่ง | ขนาด |
|---|---|---|
| `SKILL.md` | `.fix/FIG_V4/ZyntroAI_Organization/SKILL.md` | 159 บรรทัด |
| เอกสารอ้างอิง | `FIG_V4_260914_194917.PDF` (OneDrive) | — |

ชุด `docs/fig/` นี้แปลงเนื้อหาจาก `SKILL.md` เท่านั้น
เอกสาร `เอกสารเพิ่มเติม` ในโฟลเดอร์เดียวกันเป็นเรื่อง **book-to-skill / MCP tools /
invoice audit** ไม่เกี่ยวกับ FIG framework — จึงแยกออก

## สถานะการยืนยัน

| ประเด็น | สถานะ |
|---|---|
| ค่า config ทั้งหมด | ✅ ถอดจากต้นฉบับตรง |
| โครง 13 components | ✅ ถอดจากต้นฉบับตรง |
| Role capabilities | ✅ ถอดจากต้นฉบับตรง |
| FIG เป็น framework หรือ spec | ❌ **ยังไม่ยืนยัน** |
| ต้องมี `FIG.jsx` ไหม | ❌ **ยังไม่ยืนยัน** |
| ลำดับการตรวจ security | ⚠️ เป็นข้อเสนอ ไม่ใช่ต้นฉบับ |
| สถานะ GitHub rules จริง | ⚠️ ต้องเปิด branch protection ตรวจ |

## ข้อที่ต้องตัดสินก่อน implement

1. **FIG เป็นโค้ดหรือเอกสาร** — ถ้าเป็น spec จบที่ไฟล์ชุดนี้ ไม่ต้องมี `.jsx`
   ถ้าเป็น framework ต้องรู้ว่า repo ไหนเป็นที่ตั้งจริง
   (จากบันทึก: `components/FIG.v1.jsx` และ `components/FIG.tsx` อยู่ใน
   `ZyntroAI/new-radix-nova-project` — **ควรตรวจซ้ำก่อนเชื่อ**)
2. **สร้าง `.jsx` จริงไหม** — ค่าทั้งหมดในชุดนี้เป็น config ล้วน
   ถ้าต้องการ component ต้องแยกไฟล์ `FigProvider.jsx` ไม่ใช่ยัด config ใน jsx
3. **`.fix/` เป็นที่เก็บถาวรหรือไม่** — แนะนำย้ายต้นฉบับเข้า `docs/fig/`
   แล้วลบ `.fix/FIG_V4/` เพื่อไม่ให้มีสองชุดที่ต่างกัน
