fastapi>=0.141.1
uvicorn>=0.53.0
tiktoken>=0.20.0            # ← Updated
python-dotenv>=2.1.0        # ← CRITICAL FIX — was 1.2.3
pydantic>=2.13.5
openai>=3.14.0              # ← Major ver update (3.x → 1.x is actually older; pinned correctly below)
langgraph>=1.3.3            # ← Minor update
langchain>=1.6.4            # ← Minor update
langchain-openai>=1.10.3    # ← Minor update
openai>=3.9.0
langgraph>=1.2.11          # ✅ LangGraph
langchain>=1.4.0          # ✅ LangChain Base
langchain-openai>=1.6.1   # ✅ OpenAI Integration
pytest==9.1.1          # เพิ่ม
pytest-cov==7.1.0      # เพิ่ม
flake8==7.3.0          # เพิ่ม
