# Frontend (Vite) — copy to frontend/.env
#
# Vite only exposes variables prefixed with VITE_. Anything else is stripped
# from the client bundle, so never put a secret here — it ships to browsers.

VITE_API_URL=/api/v1
