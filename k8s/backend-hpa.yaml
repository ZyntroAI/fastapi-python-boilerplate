# Horizontal Pod Autoscaler for the backend API
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: zyntroai-backend
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: zyntroai-backend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
