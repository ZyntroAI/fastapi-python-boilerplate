image:
  repository: ghcr.io/your-org/oauth-api
  tag: latest

baseUrl: "https://oauth.example.com"
oauth:
  provider: "google"
  scopes: "openid email profile"
  callbackPath: "/api/auth/callback"

service:
  port: 80
